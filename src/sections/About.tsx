import styles from '@/styles'
import React from 'react'
import { motion } from "framer-motion";
import {
  navVariants,
  slideIn,
  staggerContainer,
  textVariant,
} from "@/utils/motion";



const About = () => {
  return (
    <section className={`${styles.paddings} relative x-10`}>
      <div className='gradient-02 x-0'/>
    </section>
  )
}

export default About